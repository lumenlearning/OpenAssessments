# To run:
# foreman start -f Procfile.dev

web: rails server -p $APP_PORT
webpack: cd client && nodemon webpack.hot.js

# localtunnel.me
lt_webpack: lt --subdomain $ASSETS_SUBDOMAIN --port $ASSETS_PORT
lt_rails: lt --subdomain $APP_SUBDOMAIN --port $APP_PORT