"use strict";

import React          from 'react';

export default class ItemResult extends React.Component{
  
  getStyles(props, theme){

  }
  render() {
    var styles = getStyles(this.props, this.context.theme)
    return (
      
    );
  }

}